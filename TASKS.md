# Tasks

## P0

## P1
- [ ] Recover cleanly from TASKS.md-only rebase conflicts during git sync
  **ID**: recover-from-tasks-md-sync-conflicts
  **Tags**: bug, git, tasks, multi-agent
  **Details**: Log review found `/Users/fivanishche/apps/oncall-hub-app` hitting `git_sync rebase_failed` because concurrent agents both edited `TASKS.md`. Taskgrind should treat queue-file conflicts as a common multi-agent case and recover without leaving the repo mid-rebase or silently degrading future sessions.
  **Reviewed 2026-04-12 session 7**: `taskgrind-2026-04-12-0806-oncall-hub-app-21210.log` still shows the same queue-file failure mode during the 08:06 fan-out: session 5 hit `git_sync rebase_failed` on `TASKS.md CONFLICT (content)` immediately after an audit session that added and removed temporary task blocks. This remains a current multi-agent rebase recovery gap, not just a stale historical log.
  **Reviewed 2026-04-12 session 13**: The current fan-out now reproduces the same failure in `taskgrind` itself, not just downstream repos. `taskgrind-2026-04-12-0806-taskgrind-19844.log` shows session 5 ending with `git_sync rebase_failed` on `TASKS.md CONFLICT (content)` while replaying `fix: guard expired deadline relaunch`, followed by `git_sync rebase_aborted` and `pre_session_recovery rebase_aborted` before later sessions continue shipping. That means the recovery path avoids a permanently wedged worktree, but the core queue-file conflict is still reproducible and still needs a first-class resolution path plus clearer operator logging.
  **Reviewed 2026-04-12 session 15**: The latest oncall-hub-app run keeps the same conflict alive after more task-removal churn. `taskgrind-2026-04-12-0806-oncall-hub-app-21210.log` session 15 added and removed a focused deploy-env task block, then ended with `git_sync rebase_failed` on `TASKS.md CONFLICT (content)` and `git_sync rebase_aborted` again before session 16 resumed. The worktree recovery is good enough to keep the repo moving, but the TASKS-only conflict path is still reproducible in a fresh log after multiple downstream task edits.
  **Reviewed 2026-04-12 session 16**: The current cross-repo health check suggests the existing abort-and-recover path is containing fallout, but not solving the root cause. `taskgrind`, `agentbrew`, `bosun`, `ideas`, and `oncall-hub-app` all currently report a clean git state with no `REBASE_HEAD`, and every `.taskgrind-state` file is back to `consecutive_zero_ship=0`; however that just means the fleet recovered after the last conflict. The open gap is still first-class handling for the next TASKS-only rebase collision so operators get a cleaner explanation than "rebase failed, later sessions happened to recover."
  **Reviewed 2026-04-12 session 20**: `taskgrind-2026-04-12-0807-bosun-22061.log` confirms the same queue-file collision is still happening in high-throughput repos. Sessions 5 and 10 both ended with `git_sync rebase_failed` on `TASKS.md CONFLICT (content)` while replaying the audit commit `chore: record tmux session-loss audit gap AIFN-720`, then immediately logged `git_sync rebase_aborted` before later sessions resumed. That broadens the repro from `oncall-hub-app` and `taskgrind` to `bosun`, so the issue is clearly a fleet-level TASKS churn gap rather than one repo's local workflow.
  **Reviewed 2026-04-12 session 21**: `/var/folders/vp/xnc0myyn4dsb7trvmq61j4hw0000gp/T/taskgrind-2026-04-12-0806-oncall-hub-app-21210.log` strengthens the same conclusion: the run hit four separate `TASKS.md CONFLICT (content)` rebases at 08:27, 09:11, 09:41, and 10:19, first while replaying `chore: remove completed ipsr pr task AIFN-720` and later while replaying `chore: decompose deploy-e2e into sub-tasks`. That shows the failure is not tied to one specific commit shape; ordinary queue churn in either task removal or decomposition is still enough to trigger the same TASKS-only rebase trap.
  **Reviewed 2026-04-12 session 22**: The same collision pattern is still present when comparing the highest-churn repos side by side. `/var/folders/vp/xnc0myyn4dsb7trvmq61j4hw0000gp/T/taskgrind-2026-04-12-0806-oncall-hub-app-21210.log` shows repeated `TASKS.md CONFLICT (content)` failures while replaying both task-removal and task-decomposition commits, and `/var/folders/vp/xnc0myyn4dsb7trvmq61j4hw0000gp/T/taskgrind-2026-04-12-0807-bosun-22061.log` shows the same queue-only rebase failure while replaying `chore: record tmux session-loss audit gap AIFN-720`. That keeps the root cause clearly in ordinary TASKS churn across multiple repos, not in one downstream workflow.
  **Reviewed 2026-04-12 session 23**: A full pass over every preserved 08:06/08:07 fleet log still leaves this as the dominant rebase failure class. Queue-heavy repos hit eight `TASKS.md CONFLICT (content)` rebases total across `/var/folders/vp/xnc0myyn4dsb7trvmq61j4hw0000gp/T/taskgrind-2026-04-12-0806-oncall-hub-app-21210.log` (4), `/var/folders/vp/xnc0myyn4dsb7trvmq61j4hw0000gp/T/taskgrind-2026-04-12-0806-taskgrind-19844.log` (2), and `/var/folders/vp/xnc0myyn4dsb7trvmq61j4hw0000gp/T/taskgrind-2026-04-12-0807-bosun-22061.log` (2), and every one still ends in the same abort-and-recover path rather than a first-class queue-conflict resolution. The fleet recovers, but the next ordinary TASKS churn is still set up to repeat the same failure.
  **Reviewed 2026-04-12 session 24**: Re-running the log sweep plus a live `.taskgrind-state` check across `agentbrew`, `bosun`, `ideas`, `oncall-hub-app`, and `taskgrind` keeps the same root cause open. The preserved 08:06/08:07 logs still show eight queue-only `TASKS.md CONFLICT (content)` rebases across `oncall-hub-app`, `taskgrind`, and `bosun`, while the current worktrees are clean and only `bosun` still carries `consecutive_zero_ship=2`. That means recovery is containing fallout today, but taskgrind still lacks a first-class response for the next routine `TASKS.md` collision.
  **Reviewed 2026-04-12 session 25**: The latest full sweep still shows the same queue-only conflict class, just with updated live state. The preserved 08:06/08:07 logs still contain eight `TASKS.md CONFLICT (content)` rebases across `/var/folders/vp/xnc0myyn4dsb7trvmq61j4hw0000gp/T/taskgrind-2026-04-12-0806-oncall-hub-app-21210.log` (4), `/var/folders/vp/xnc0myyn4dsb7trvmq61j4hw0000gp/T/taskgrind-2026-04-12-0806-taskgrind-19844.log` (1), and `/var/folders/vp/xnc0myyn4dsb7trvmq61j4hw0000gp/T/taskgrind-2026-04-12-0807-bosun-22061.log` (3), while the current `.taskgrind-state` files show `consecutive_zero_ship=3` in `agentbrew`, `1` in `ideas`, and `0` in `bosun`, `oncall-hub-app`, and `taskgrind`. The fleet is recovering after each abort, but taskgrind still has no first-class handling for the next routine `TASKS.md` collision.
  **Reviewed 2026-04-12 session 26**: Re-checking the preserved conflict logs plus the live `.taskgrind-state` files keeps the same queue-only rebase gap open. The 08:06/08:07 slice still shows eight `TASKS.md CONFLICT (content)` rebases split across `/var/folders/vp/xnc0myyn4dsb7trvmq61j4hw0000gp/T/taskgrind-2026-04-12-0806-oncall-hub-app-21210.log` (4), `/var/folders/vp/xnc0myyn4dsb7trvmq61j4hw0000gp/T/taskgrind-2026-04-12-0806-taskgrind-19844.log` (1), and `/var/folders/vp/xnc0myyn4dsb7trvmq61j4hw0000gp/T/taskgrind-2026-04-12-0807-bosun-22061.log` (3), while the current state files now read `consecutive_zero_ship=3` in `agentbrew`, `1` in `ideas`, and `0` in `bosun`, `oncall-hub-app`, and `taskgrind`. Recovery is still containing fallout after each abort, but taskgrind still lacks a first-class response for the next routine queue-file collision.
  **Reviewed 2026-04-12 session 27**: Another preserved-log and live-state pass keeps the same queue-only rebase gap open. The 08:06/08:07 slice still shows eight `TASKS.md CONFLICT (content)` rebases across `/var/folders/vp/xnc0myyn4dsb7trvmq61j4hw0000gp/T/taskgrind-2026-04-12-0806-oncall-hub-app-21210.log` (4), `/var/folders/vp/xnc0myyn4dsb7trvmq61j4hw0000gp/T/taskgrind-2026-04-12-0806-taskgrind-19844.log` (1), and `/var/folders/vp/xnc0myyn4dsb7trvmq61j4hw0000gp/T/taskgrind-2026-04-12-0807-bosun-22061.log` (3), while `/var/folders/vp/xnc0myyn4dsb7trvmq61j4hw0000gp/T/taskgrind-2026-04-12-0806-agentbrew-19027.log` still provides the contrasting non-queue `docs/COMPETITION.md` conflict. Current `.taskgrind-state` files show `consecutive_zero_ship=0` in `agentbrew`, `oncall-hub-app`, and `taskgrind`, `1` in `bosun`, and `2` in `ideas`, so recovery is containing fallout today but taskgrind still has no first-class response for the next routine `TASKS.md` collision.
  **Files**: `bin/taskgrind`, `tests/git-sync.bats`, `tests/resume.bats`
  **Acceptance**: Add a failing test first; a rebase conflict isolated to `TASKS.md` is either auto-resolved safely or aborted with a clean recovery path; later sessions start from a healthy git state and the logs explain what happened.
## P2
- [ ] Drop stale skip-threshold history when task IDs disappear from the queue
  **ID**: prune-stale-skipped-task-attempts
  **Tags**: bug, tasks, logging, multi-agent
  **Details**: The latest logs still emit `task_skip_threshold` warnings for task IDs that are no longer actionable in the live queue. `/var/folders/vp/xnc0myyn4dsb7trvmq61j4hw0000gp/T/taskgrind-2026-04-12-0806-agentbrew-19027.log` and `/var/folders/vp/xnc0myyn4dsb7trvmq61j4hw0000gp/T/taskgrind-2026-04-12-0806-agentbrew-19576.log` both report `watch-skills-cli-mcp-may-2026` even though that ID is gone from `agentbrew/TASKS.md`, and `/var/folders/vp/xnc0myyn4dsb7trvmq61j4hw0000gp/T/taskgrind-2026-04-12-0806-taskgrind-20411.log` still reports removed `taskgrind` IDs such as `log-productive-zero-ship-cause`. Taskgrind should prune or expire attempt history when a task ID disappears so operators do not chase stale skip-threshold noise from already-removed work.
  **Reviewed 2026-04-12 session 21**: The stale-history noise is still active across both downstream and self-hosted runs. `/var/folders/vp/xnc0myyn4dsb7trvmq61j4hw0000gp/T/taskgrind-2026-04-12-0806-ideas-17272.log` still warns on `control-tower-boundary-review`, and `/var/folders/vp/xnc0myyn4dsb7trvmq61j4hw0000gp/T/taskgrind-2026-04-12-0806-taskgrind-19844.log` plus `/var/folders/vp/xnc0myyn4dsb7trvmq61j4hw0000gp/T/taskgrind-2026-04-12-0806-taskgrind-20411.log` continue surfacing removed IDs such as `align-audit-target-with-sweep-contract`, `document-full-tasks-md-metadata`, and `log-productive-zero-ship-cause`. That confirms the attempt ledger is outliving queue reality in more than one repo.
  **Reviewed 2026-04-12 session 22**: The stale-attempt pattern also shows up in another repo, not just `ideas` and `taskgrind`. `/var/folders/vp/xnc0myyn4dsb7trvmq61j4hw0000gp/T/taskgrind-2026-04-12-0806-agentbrew-19027.log` emits `task_skip_threshold ids=watch-skills-cli-mcp-may-2026`, and that ID is no longer present in `/Users/fivanishche/apps/agentbrew/TASKS.md`. With stale IDs now visible in `agentbrew`, `ideas`, and `taskgrind`, the skip-history leak is clearly fleet-wide.
  **Reviewed 2026-04-12 session 23**: The fresh pass narrows the repro but does not clear it. `/var/folders/vp/xnc0myyn4dsb7trvmq61j4hw0000gp/T/taskgrind-2026-04-12-0806-ideas-17272.log` now warns on live `ideas` tasks (`control-tower-receipt-history` and `control-tower-refresh-node-types`), so `ideas` no longer proves stale history by itself; however both preserved `agentbrew` logs still emit removed `watch-skills-cli-mcp-may-2026`, and `/var/folders/vp/xnc0myyn4dsb7trvmq61j4hw0000gp/T/taskgrind-2026-04-12-0806-taskgrind-20411.log` still warns on removed `log-productive-zero-ship-cause`. The bug remains real, but the current live examples are now `agentbrew` plus `taskgrind`, not `ideas`.
  **Reviewed 2026-04-12 session 24**: Re-checking the preserved logs against the current queues keeps the stale-history repro scoped to two repos. `ideas` no longer strengthens this bug because its latest threshold warnings (`control-tower-receipt-history` and `control-tower-refresh-node-types`) still exist in `/Users/fivanishche/apps/ideas/TASKS.md`, but both `agentbrew` logs still warn on removed `watch-skills-cli-mcp-may-2026`, and the `taskgrind` logs still warn on removed IDs such as `align-audit-target-with-sweep-contract`, `document-full-tasks-md-metadata`, and `log-productive-zero-ship-cause`. The stale-attempt ledger is still real; the current repro set is just narrower than earlier passes suggested.
  **Reviewed 2026-04-12 session 25**: Re-running the threshold audit keeps the stale-history bug narrowed but active. `ideas` no longer counts as stale because `/var/folders/vp/xnc0myyn4dsb7trvmq61j4hw0000gp/T/taskgrind-2026-04-12-0806-ideas-17272.log` now warns only on task IDs that still exist in `/Users/fivanishche/apps/ideas/TASKS.md`, but both preserved `agentbrew` logs still emit removed `watch-skills-cli-mcp-may-2026`, and the preserved `taskgrind` logs still emit removed IDs including `align-audit-target-with-sweep-contract`, `document-full-tasks-md-metadata`, and `log-productive-zero-ship-cause`. The attempt ledger is still outliving queue reality in at least `agentbrew` and `taskgrind`.
  **Reviewed 2026-04-12 session 26**: Another threshold audit keeps the stale-history repro narrow but still active. `/var/folders/vp/xnc0myyn4dsb7trvmq61j4hw0000gp/T/taskgrind-2026-04-12-0806-ideas-17272.log` still no longer qualifies as stale because its warned IDs (`control-tower-receipt-history` and `control-tower-refresh-node-types`) are still present in `/Users/fivanishche/apps/ideas/TASKS.md`, but both preserved `agentbrew` logs still emit removed `watch-skills-cli-mcp-may-2026`, and the preserved `taskgrind` logs still emit removed IDs including `align-audit-target-with-sweep-contract`, `document-full-tasks-md-metadata`, and `log-productive-zero-ship-cause`. The attempt ledger is still outliving queue reality in at least `agentbrew` and `taskgrind`.
  **Files**: `bin/taskgrind`, `tests/resume.bats`, `tests/logging.bats`
  **Acceptance**: Add a failing test first; removed task IDs stop contributing to future `task_skip_threshold` output; active long-lived skipped tasks still retain their attempt history; logs stay focused on currently actionable queue items.
## P3
