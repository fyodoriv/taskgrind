# Tasks

## P0

## P1
- [ ] Detect each repo's real sync base instead of assuming `main`
  **ID**: detect-sync-base-without-main
  **Tags**: bug, git, branch, multi-agent
  **Details**: Log review found repeated sync cleanup failures in `/Users/fivanishche/apps/ideas`, where `/var/folders/vp/xnc0myyn4dsb7trvmq61j4hw0000gp/T/taskgrind-2026-04-12-0806-ideas-17272.log` hit `git_sync checkout_failed: error: pathspec 'main' did not match any file(s) known to git` in sessions 5, 10, 15, 20, and 25. That repo currently tracks `standing-ideas-gap-loop`, and `git remote show origin` reports `HEAD branch: (unknown)`, so taskgrind cannot safely assume every repo has a local `main` branch when it returns to the base branch for sync or cleanup.
  **Reviewed 2026-04-12 session 31**: Re-checking the live repo confirms the failure is still actionable, not just preserved log noise. `/Users/fivanishche/apps/ideas` is still on `standing-ideas-gap-loop`, its `origin` remote still reports `HEAD branch: (unknown)`, and the preserved log still shows five `pathspec 'main'` checkout failures spread across the 08:06 fan-out. Taskgrind needs a first-class fallback to the repo's configured upstream or current branch so non-`main` repos do not keep skipping sync cleanup.
  **Files**: `bin/taskgrind`, `tests/git-sync.bats`, `tests/resume.bats`
  **Acceptance**: Add a failing test first; git sync chooses a valid base branch in repos without a local `main`; cleanup no longer logs `pathspec 'main' did not match any file(s) known to git`; logs explain which branch taskgrind selected when the remote HEAD is unavailable.
## P2
- [ ] Drop stale skip-threshold history when task IDs disappear from the queue
  **ID**: prune-stale-skipped-task-attempts
  **Tags**: bug, tasks, logging, multi-agent
  **Details**: The latest logs still emit `task_skip_threshold` warnings for task IDs that are no longer actionable in the live queue. `/var/folders/vp/xnc0myyn4dsb7trvmq61j4hw0000gp/T/taskgrind-2026-04-12-0806-agentbrew-19027.log` and `/var/folders/vp/xnc0myyn4dsb7trvmq61j4hw0000gp/T/taskgrind-2026-04-12-0806-agentbrew-19576.log` both report `watch-skills-cli-mcp-may-2026` even though that ID is gone from `agentbrew/TASKS.md`, and `/var/folders/vp/xnc0myyn4dsb7trvmq61j4hw0000gp/T/taskgrind-2026-04-12-0806-taskgrind-20411.log` still reports removed `taskgrind` IDs such as `log-productive-zero-ship-cause`. Taskgrind should prune or expire attempt history when a task ID disappears so operators do not chase stale skip-threshold noise from already-removed work.
  **Reviewed 2026-04-12 session 21**: The stale-history noise is still active across both downstream and self-hosted runs. `/var/folders/vp/xnc0myyn4dsb7trvmq61j4hw0000gp/T/taskgrind-2026-04-12-0806-ideas-17272.log` still warns on `control-tower-boundary-review`, and `/var/folders/vp/xnc0myyn4dsb7trvmq61j4hw0000gp/T/taskgrind-2026-04-12-0806-taskgrind-19844.log` plus `/var/folders/vp/xnc0myyn4dsb7trvmq61j4hw0000gp/T/taskgrind-2026-04-12-0806-taskgrind-20411.log` continue surfacing removed IDs such as `align-audit-target-with-sweep-contract`, `document-full-tasks-md-metadata`, and `log-productive-zero-ship-cause`. That confirms the attempt ledger is outliving queue reality in more than one repo.
  **Reviewed 2026-04-12 session 22**: The stale-attempt pattern also shows up in another repo, not just `ideas` and `taskgrind`. `/var/folders/vp/xnc0myyn4dsb7trvmq61j4hw0000gp/T/taskgrind-2026-04-12-0806-agentbrew-19027.log` emits `task_skip_threshold ids=watch-skills-cli-mcp-may-2026`, and that ID is no longer present in `/Users/fivanishche/apps/agentbrew/TASKS.md`. With stale IDs now visible in `agentbrew`, `ideas`, and `taskgrind`, the skip-history leak is clearly fleet-wide.
  **Reviewed 2026-04-12 session 23**: The fresh pass narrows the repro but does not clear it. `/var/folders/vp/xnc0myyn4dsb7trvmq61j4hw0000gp/T/taskgrind-2026-04-12-0806-ideas-17272.log` now warns on live `ideas` tasks (`control-tower-receipt-history` and `control-tower-refresh-node-types`), so `ideas` no longer proves stale history by itself; however both preserved `agentbrew` logs still emit removed `watch-skills-cli-mcp-may-2026`, and `/var/folders/vp/xnc0myyn4dsb7trvmq61j4hw0000gp/T/taskgrind-2026-04-12-0806-taskgrind-20411.log` still warns on removed `log-productive-zero-ship-cause`. The bug remains real, but the current live examples are now `agentbrew` plus `taskgrind`, not `ideas`.
  **Reviewed 2026-04-12 session 24**: Re-checking the preserved logs against the current queues keeps the stale-history repro scoped to two repos. `ideas` no longer strengthens this bug because its latest threshold warnings (`control-tower-receipt-history` and `control-tower-refresh-node-types`) still exist in `/Users/fivanishche/apps/ideas/TASKS.md`, but both `agentbrew` logs still warn on removed `watch-skills-cli-mcp-may-2026`, and the `taskgrind` logs still warn on removed IDs such as `align-audit-target-with-sweep-contract`, `document-full-tasks-md-metadata`, and `log-productive-zero-ship-cause`. The stale-attempt ledger is still real; the current repro set is just narrower than earlier passes suggested.
  **Reviewed 2026-04-12 session 25**: Re-running the threshold audit keeps the stale-history bug narrowed but active. `ideas` no longer counts as stale because `/var/folders/vp/xnc0myyn4dsb7trvmq61j4hw0000gp/T/taskgrind-2026-04-12-0806-ideas-17272.log` now warns only on task IDs that still exist in `/Users/fivanishche/apps/ideas/TASKS.md`, but both preserved `agentbrew` logs still emit removed `watch-skills-cli-mcp-may-2026`, and the preserved `taskgrind` logs still emit removed IDs including `align-audit-target-with-sweep-contract`, `document-full-tasks-md-metadata`, and `log-productive-zero-ship-cause`. The attempt ledger is still outliving queue reality in at least `agentbrew` and `taskgrind`.
  **Reviewed 2026-04-12 session 26**: Another threshold audit keeps the stale-history repro narrow but still active. `/var/folders/vp/xnc0myyn4dsb7trvmq61j4hw0000gp/T/taskgrind-2026-04-12-0806-ideas-17272.log` still no longer qualifies as stale because its warned IDs (`control-tower-receipt-history` and `control-tower-refresh-node-types`) are still present in `/Users/fivanishche/apps/ideas/TASKS.md`, but both preserved `agentbrew` logs still emit removed `watch-skills-cli-mcp-may-2026`, and the preserved `taskgrind` logs still emit removed IDs including `align-audit-target-with-sweep-contract`, `document-full-tasks-md-metadata`, and `log-productive-zero-ship-cause`. The attempt ledger is still outliving queue reality in at least `agentbrew` and `taskgrind`.
  **Files**: `bin/taskgrind`, `tests/resume.bats`, `tests/logging.bats`
  **Acceptance**: Add a failing test first; removed task IDs stop contributing to future `task_skip_threshold` output; active long-lived skipped tasks still retain their attempt history; logs stay focused on currently actionable queue items.
## P3
